# uml_mcp

UML Markdown 与代码同步工作流：四阶段文档在 `ai-software-engineering/`，Vue SDI 实现在 `uml-vue-sdi/`，示例与 `uml.sync.md` 在 `examples/sync-demo/`。

## 快速开始

```bash
cd uml-vue-sdi
npm install
npm run dev
```

Cursor 中 UML ↔ 代码协同规则：`.cursor/rules/uml-code-sync.mdc`。

## 许可证

见 `THIRD_PARTY_LICENSES.md`。
