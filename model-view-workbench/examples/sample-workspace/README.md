# sample-workspace

单文件 [`models.md`](models.md)：演示 **Model / View / Map** 围栏组合，用于在浏览器或 Electron 中打开本文件夹后快速点选代码块、打开各代码块画布。

- 依赖：`@mvwb/core` 对 `mv-model-sql` / `mv-view` / `mv-map` 的解析（无外部服务）。
- 不包含 `mv-model-codespace`；若需代码空间 UML 示意，见上级目录下的 [`codespace-demo/`](../codespace-demo/)。
