# codespace-demo

最小工作区：单文件 [`codespace.md`](codespace.md)，仅含一个 **`smw-model-codespace`** 围栏，覆盖递归命名空间、继承（`bases`）、类级 `associations`、命名空间级 `functions` / `macros`。

用于回归「打开文件夹 → 解析无错误 → 代码块大纲 / 属性 Dock 摘要」等路径。
